<Window x:Class="ControlShutter.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ControlShutter.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="800" Width="1200"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Icon="/Assets/app.ico">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- 样式定义 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF2196F3"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="StatusBarStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF263238"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF0D47A1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFE0E0E0"/>
                                <Setter Property="Foreground" Value="#FF9E9E9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF388E3C"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF1B5E20"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#FFF44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFD32F2F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFB71C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#FF2196F3" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Image Source="/Assets/Images/logo.png" Height="32" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" 
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Ellipse Width="12" Height="12" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="Red"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="LimeGreen"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="White" 
                               VerticalAlignment="Center" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧控制面板 -->
            <Border Grid.Column="0" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0" BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">
                        <!-- 设备选择 -->
                        <GroupBox Header="设备选择" Margin="0,0,0,20">
                            <StackPanel Margin="10">
                                <ComboBox ItemsSource="{Binding DeviceTypes}" 
                                          SelectedItem="{Binding SelectedDeviceType}"
                                          FontSize="14" Padding="8"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- 门控制 -->
                        <GroupBox Header="门控制" Margin="0,0,0,20">
                            <StackPanel Margin="10">
                                <Button Content="🚪 开门" 
                                        Command="{Binding OpenDoorCommand}" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        IsEnabled="{Binding IsConnected}"/>
                                
                                <Button Content="🔒 关门" 
                                        Command="{Binding CloseDoorCommand}" 
                                        Style="{StaticResource DangerButtonStyle}"
                                        IsEnabled="{Binding IsConnected}"/>
                                
                                <Button Content="🔄 刷新状态" 
                                        Command="{Binding RefreshStatusCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                                
                                <Button Content="🔗 检查连接" 
                                        Command="{Binding CheckConnectionCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- 快速导航 -->
                        <GroupBox Header="快速导航" Margin="0,0,0,20">
                            <StackPanel Margin="10">
                                <Button Content="📊 门控制面板" 
                                        Command="{Binding NavigateToDoorControlCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                                
                                <Button Content="📈 设备状态监控" 
                                        Command="{Binding NavigateToDeviceStatusCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                                
                                <Button Content="⚙️ 系统设置" 
                                        Command="{Binding NavigateToSettingsCommand}" 
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- 设备状态 -->
                        <GroupBox Header="设备状态">
                            <StackPanel Margin="10">
                                <ItemsControl ItemsSource="{Binding DeviceStatuses}"
                                              ItemTemplate="{StaticResource DeviceStatusTemplate}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- 右侧主要内容区域 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <!-- 动态内容区域 -->
                <Border Grid.Row="0" Background="White" Margin="10">
                    <ContentPresenter Content="{Binding CurrentView}">
                        <ContentPresenter.ContentTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding}"/>
                            </DataTemplate>
                        </ContentPresenter.ContentTemplate>
                    </ContentPresenter>
                </Border>

                <!-- 操作日志 -->
                <GroupBox Grid.Row="1" Header="操作日志" Margin="10,0,10,10">
                    <DataGrid ItemsSource="{Binding OperationLogs}" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              RowHeaderWidth="0"
                              FontSize="12">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="状态" Binding="{Binding StatusIcon}" Width="40"/>
                            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}" Width="80"/>
                            <DataGridTextColumn Header="操作" Binding="{Binding Operation}" Width="100"/>
                            <DataGridTextColumn Header="设备" Binding="{Binding DeviceType}" Width="100"/>
                            <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                        <Setter Property="Background" Value="#FFFFE5E5"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                        <Setter Property="Background" Value="#FFE5FFE5"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </GroupBox>
            </Grid>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Style="{StaticResource StatusBarStyle}">
            <Grid Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                           Foreground="White" VerticalAlignment="Center" FontSize="12"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <!-- 加载指示器 -->
                    <Ellipse Width="12" Height="12" Fill="Orange" Margin="0,0,10,0"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Ellipse.RenderTransform>
                            <RotateTransform CenterX="6" CenterY="6"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>

                    <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='HH:mm:ss'}" 
                               Foreground="White" VerticalAlignment="Center" FontSize="12"
                               xmlns:sys="clr-namespace:System;assembly=mscorlib"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>